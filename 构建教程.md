# DockerCopilot 构建教程

本文说明如何在 Debian / Windows 环境下完成：

1. 前端构建
2. Windows EXE 构建
3. Linux amd64 二进制构建
4. Linux arm64 二进制构建
5. Debian 一键交互式构建脚本使用方式
6. Windows CMD 一键构建脚本使用方式

---

## 1. 项目目录

请先进入项目根目录：

```bash
cd dockerCopilot
```

---

## 2. 前端构建

### 2.1 构建 PC + Mobile 前端

```bash
npm install
npm run build:front
```

构建结果：

- PC 前端输出到 [`front/pc`](front/pc)
- Mobile 前端输出到 [`front/mobile`](front/mobile)

### 2.2 仅构建移动端前端

```bash
npm --prefix ./web-mobile install
npm --prefix ./web-mobile run build
node ./scripts/sync-mobile.mjs
```

### 2.3 移动端发布目录

移动端静态文件最终同步到：

- [`front/mobile`](front/mobile)

---

## 3. 二进制构建

项目后端会把前端资源嵌入到可执行文件中，所以**建议先构建前端，再构建二进制**。

### 3.1 Windows EXE

```bash
CGO_ENABLED=0 GOOS=windows GOARCH=amd64 \
  go build -a --trimpath \
  -ldflags="-w -s -X github.com/onlyLTY/dockerCopilot/internal/config.Version=$(cat version) -X github.com/onlyLTY/dockerCopilot/internal/config.BuildDate=$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
  -o release/windows/amd64/dockerCopilot.exe .
```

### 3.2 Linux amd64

```bash
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
  go build -a --trimpath \
  -ldflags="-w -s -X github.com/onlyLTY/dockerCopilot/internal/config.Version=$(cat version) -X github.com/onlyLTY/dockerCopilot/internal/config.BuildDate=$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
  -o release/linux/amd64/dockerCopilot .
```

### 3.3 Linux arm64

```bash
CGO_ENABLED=0 GOOS=linux GOARCH=arm64 \
  go build -a --trimpath \
  -ldflags="-w -s -X github.com/onlyLTY/dockerCopilot/internal/config.Version=$(cat version) -X github.com/onlyLTY/dockerCopilot/internal/config.BuildDate=$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
  -o release/linux/arm64/dockerCopilot .
```

### 3.4 输出目录

- Windows EXE：[`release/windows/amd64`](release/windows/amd64)
- Linux amd64：[`release/linux/amd64`](release/linux/amd64)
- Linux arm64：[`release/linux/arm64`](release/linux/arm64)

---

## 4. Debian 一键构建脚本

脚本路径：

- [`scripts/build-debian.sh`](scripts/build-debian.sh)

功能：

- 自动检查 Debian 基础依赖
- 自动安装缺失依赖
- 自动补齐 Node.js 和 Go（若本机版本不足）
- 自动构建 PC + Mobile 前端
- 交互选择目标二进制：
  - `1` Windows amd64 EXE
  - `2` Linux amd64
  - `3` Linux arm64

### 4.1 执行方式

```bash
chmod +x scripts/build-debian.sh
bash scripts/build-debian.sh
```

### 4.2 交互选项

脚本会提示你输入：

- `1`：构建 Windows amd64 EXE
- `2`：构建 Linux amd64
- `3`：构建 Linux arm64

### 4.3 构建输出

默认输出到：

- [`release/windows/amd64/dockerCopilot.exe`](release/windows/amd64)
- [`release/linux/amd64/dockerCopilot`](release/linux/amd64)
- [`release/linux/arm64/dockerCopilot`](release/linux/arm64)

---

## 5. Windows CMD 一键构建脚本

脚本路径：

- [`scripts/build-windows.cmd`](scripts/build-windows.cmd)

功能：

- 自动检查 `node` / `npm.cmd` / `go`
- 自动尝试补充常见的 Node.js 与 Go 安装路径
- 自动安装前端依赖
- 自动构建 PC + Mobile 前端
- 交互选择目标二进制：
  - `1` Windows amd64 EXE
  - `2` Linux amd64
  - `3` Linux arm64

### 5.1 执行方式

在 Windows 的 `cmd.exe` 中进入项目根目录后执行：

```bat
scripts\build-windows.cmd
```

也可以双击直接运行 [`scripts/build-windows.cmd`](scripts/build-windows.cmd)。

### 5.2 交互选项

脚本会提示你输入：

- `1`：构建 [`release/windows/amd64/dockerCopilot.exe`](release/windows/amd64)
- `2`：构建 [`release/linux/amd64/dockerCopilot`](release/linux/amd64)
- `3`：构建 [`release/linux/arm64/dockerCopilot`](release/linux/arm64)

### 5.3 前置要求

- 建议已安装 Node.js 20+
- 建议已安装 Go 1.24.1+
- 若命令未进 PATH，脚本会尝试补充常见安装路径
- 若仍失败，请手动安装后重新运行

---

## 6. 推荐构建顺序

如果你要发布最新版本，建议按以下顺序执行：

### Debian

1. `bash scripts/build-debian.sh`
2. 选择目标架构 `1 / 2 / 3`
3. 检查 [`front/pc`](front/pc) 与 [`front/mobile`](front/mobile)
4. 检查对应二进制输出目录

### Windows

1. `scripts\build-windows.cmd`
2. 选择目标架构 `1 / 2 / 3`
3. 检查 [`front/pc`](front/pc) 与 [`front/mobile`](front/mobile)
4. 检查对应二进制输出目录

---

## 7. 版本说明

版本号来源于：

- [`version`](version)
- [`internal/config.Version`](internal/config/config.go:14)

构建脚本会自动把版本号和构建时间写入二进制。

---

## 8. 常见问题

### 8.1 前端构建失败

先确认 Node.js 和 npm 是否可用：

```bash
node -v
npm -v
```

### 8.2 Go 构建失败

确认 Go 版本满足要求：

```bash
go version
```

建议使用 Go 1.24.1 或更高版本。

### 8.3 二进制没有包含最新前端

必须先执行前端构建，再执行 `go build`。

---

## 9. 总结

- 前端统一通过 [`npm run build:front`](package.json:10) 构建
- 移动端静态文件同步到 [`front/mobile`](front/mobile)
- Debian 推荐使用 [`scripts/build-debian.sh`](scripts/build-debian.sh) 一键构建
- Windows 推荐使用 [`scripts/build-windows.cmd`](scripts/build-windows.cmd) 一键构建
- Windows / Linux amd64 / Linux arm64 二进制均可通过 `GOOS` / `GOARCH` 交叉编译生成
